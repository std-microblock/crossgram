# MTProto Relay —— 架构设计与开发计划

> cordis-native 实现，仓库：`mtproto-relay-cordis`。
> 目标：做一个"镜像 Telegram 服务器"，让打过补丁的官方客户端（Telegram Desktop / TDLib）或 mtcute 连进来，把每个账户按需路由到 **官方 Telegram（relay）** 或 **各 IM 平台（bridge）**。
> 一切皆 cordis 插件；数据库用 **minato**（`@cordisjs/plugin-database`），不用 TypeORM。

---

## 1. 当前状态（2026-07-16）

| 能力 | 状态 |
|---|---|
| 混淆+abridged 传输、DH 握手、PFS 临时密钥、auth.bindTempAuthKey | ✅ 官端实测通过 |
| auth key 持久化（`FileAuthKeyStore`，跨重启复用） | ✅ |
| 虚拟号登录（HTTP 发号 → auth.sendCode/signIn） | ✅ 官端实测能登入主界面 |
| 登录后初始同步（getConfig/getDialogFilters/updates.getState/…） | ✅ 够进主界面 |
| 真实好友/会话/历史（contacts/users/messages.get*） | ✅ Bridge 平台数据已可显示 |
| 发送消息（messages.sendMessage） | ✅ 支持幂等发送与历史回读 |
| Relay 模式（透传官方 Telegram） | ⬜ → #16 |
| 多账户按后端路由 | ⬜ → #16 |
| 文件传输 | ✅ 分片落盘、adapter 流式上传、range 下载 |

**测试**：`packages/mtproto` 34 个；`packages/bridge` 65 个；独立的 `packages/test-suite` 10 个 adapter conformance 和全 socket e2e。覆盖 Cordis 多例注册、握手/PFS/跨连接恢复、IMMessage 入库、分页历史、媒体分组、流式上传/range 下载、群组和 channel/subchannel。

---

## 2. 架构

### 2.1 分层

```
Client (打补丁的 tdesktop / mtcute)
   │  TCP + obfuscated/abridged framing
   ▼
┌───────────────────────────────────────────────┐
│ ctx.mtproto  (@mtproto-relay/mtproto)          │  原生 cordis Service
│  · 直接持有 net.Server + 每连接一个 ServerSession │  (@cordisjs/plugin-server 模式)
│  · 协议：握手 / 加解密 / PFS / 容器 / ack / salt  │
│  · auth key 持久化 + 多密钥路由                  │
│  · register(method,h) / fallback(h) —— ctx.effect │  ← HMR 友好
└──────────────────┬────────────────────────────┘
                   │ 解密后的 tl.RpcMethod
                   ▼
┌───────────────────────────────────────────────┐
│ 路由层（按账户）—— 见 §3                          │
│   authKeyId → routeId → 对应后端的 handler 集      │
└──────┬───────────────────────────┬─────────────┘
       ▼                           ▼
┌──────────────┐          ┌────────────────────────┐
│ Relay 后端    │          │ Bridge 后端             │
│ 透传官方 TG   │          │ IM 平台适配（合成 TL）    │
│ (#16)        │          │  · ctx.database (minato) │
└──────────────┘          │  · ctx.server (HTTP 登录) │
                          │  · IMPlatform 抽象        │
                          └────────────────────────┘
```

### 2.2 各包职责

- **`@mtproto-relay/mtproto`** —— MTProto 协议 + 服务器 Service。对外只暴露 `ctx.mtproto`（register/fallback/broadcastUpdate/rsaKey）。协议内部件（ServerSession/ServerConnection/ServerAuthKey/握手/rsa-keygen/message-id/dispatcher）都是实现细节。
- **`@mtproto-relay/bridge`** —— IM 平台桥接插件。定义 minato 模型、HTTP 虚拟号登录、把 RPC 合成为 IM 平台操作和 `IMPlatform` 抽象。
- **`@mtproto-relay/platform-static`** —— 完整内存参考 adapter，覆盖私聊、群聊、channel/subchannel、分页历史及流式图片/文件。
- **`@mtproto-relay/test-suite`** —— 独立的跨包契约与 socket e2e 测试；单向依赖 static adapter、bridge 和 mtproto，生产包不反向依赖测试代码。
- **`@mtproto-relay/relay`**（待建）—— 透传插件：上游 `BaseTelegramClient`，`fallback` 直接转发。

### 2.3 cordis 关键机制（为什么这么设计）
- **一切皆插件 + 依赖注入**：后端 `inject: ['mtproto','database','model','server']`，cordis 自动排序启动（db 先于 bridge），依赖消失自动卸载。
- **effect = 自动清理**：handler 用 `ctx.mtproto.register()`（内部 `ctx.effect`）注册，绑定到调用方 fiber —— 热重载/卸载时 handler 自动增删，**服务器和在线连接不掉**。
- **HMR**：boilerplate 用 `@cordiverse/tsx` + `@cordisjs/plugin-hmr` 监听 `packages/`，改 handler 即时生效。
- **统一日志**：MTProto/握手/连接日志默认桥接到 `ctx.logger('mtproto/<tag>')`，由 Cordis 的日志级别、console/WebUI exporter 统一管理；仍可用 `MtprotoConfig.log` 注入自定义 mtcute logger。

---

## 3. 多账户路由设计（重点）

**需求**：一个客户端多账户，各账户路由到不同后端。例：
`账户 A → 官方 Telegram relay`、`B → relay 平台 A`、`C → relay 平台 B`。

**设计**：
1. `Mtproto` Service 内维护 **RouteRegistry**：`routeId → RpcDispatcher`。每个后端插件启动时注册一条 route（`relay:official`、`bridge:platformA`…），用 `ctx.isolate` 给各后端独立实例/配置。
2. **绑定** `authKeyId → { routeId, platformSession }`，在 `auth.signIn` 成功时确定并用 minato 持久化：
   - 虚拟号前缀决定平台（`+99901…`→平台A、`+99902…`→平台B）；
   - 真实 TG 登录 → `relay:official`。
3. **派发**：`ServerSession` 登录前用「默认 route」（help/auth 这些无账户上下文的）；登录后按本连接绑定的 `routeId` 派发到对应后端的 dispatcher。
4. **需要改 mtproto-core**：目前是全局单一 dispatcher；要改成 session 持有 `routeId`、按账户选 dispatcher。`ServerRpcContext` 增加 `routeId`。

**要点**：mtproto 传输/会话层与账户无关（共享）；「按账户路由」只发生在 RPC 派发这一层。

---

## 4. 数据模型（minato，`ctx.model.extend`）

已建：
- `mtproto_auth_session`：`{ id, virtualPhone(纯数字), loginCode, platformId, platformSessionId, used }` —— 虚拟号登录待认领。
- `mtproto_platform_session`：`{ id, platformId, userId, credentials(json), metadata(json), active, createdAt }` —— 已认证的平台会话。
- `mtproto_auth_binding`：`{ authKeyId, platformId, platformSessionId }` —— Bridge 账户绑定；同一 auth key 的多连接共享状态，服务重启后从此表恢复。

待加：
- `mtproto_auth_binding.routeId`：Relay/Bridge 多后端路由字段 —— §3 的账户→后端绑定扩展。
- ✅ `mtproto_im_conversation` / `mtproto_im_message` / alias / media / TL projection：history 与 push-only 平台的统一事实层。
- ✅ `mtproto_update_state`：`pts/qts/seq/date`（每账户）。

---

## 5. 路线图

### ✅ 已完成
- **Phase 0** 传输 + DH 握手 + PFS + auth key 持久化（协议核心，11 测试）。
- **Phase 1a** 原生 `Mtproto` Service + `cordis run` 启动 + HMR。
- **Phase 1b** Bridge 登录链路（minato 模型 + HTTP 虚拟号 + auth.sendCode/signIn + 够进主界面的初始同步）。

### ⬜ #17 Bridge 补全：真实会话与消息（下一步优先）
- ✅ **裸 `Vector` 返回支持**：`bareVector()` + `_sendRpcResult` 序列化（`0x1cb5c415`+count+items）。
- ✅ **真实 `messages.getDialogs`/`getHistory`/`getMessages`**：把 `IMPlatform` 数据构造成合法 TL（Dialog/Message/Peer/User + 正确 flags），支持数字 ID 映射、反向 peer 映射与分页过滤。
- ✅ `messages.sendMessage` → `platform.sendMessage` + `updateShortSentMessage`，按 `random_id` 幂等并校验平台能力/长度。
- ✅ `users.getUsers`/`getFullUser`、`contacts.getContacts`。
- ✅ 官端主列表合并：`getDialogs(exclude_pinned)` + `getPinnedDialogs`（返回 `messages.peerDialogs` 与 update state）。
- ✅ 官端登录后启动资源：颜色、reaction、emoji、stickers、stories、premium 等不支持能力返回合法空对象，避免 500 重试阻塞初始化。
- ✅ 官端日志请求全覆盖 e2e：含 `auth.exportLoginToken` 合法短期 token；启动批次逐项断言不返回 `mt_rpc_error`。
- ✅ **UpdateManager**：持久化 `pts/qts/seq` + `IMEvent → MTPUpdates` + 按 auth key 定向推送。
- ✅ 登录 e2e：登录 → 新连接复用 auth key → contacts/dialogs/history → sendMessage → 历史回读；另覆盖完整服务重启恢复。
- 完善 push e2e：收到平台主动消息 update。

### ⬜ #16 Relay 模式 + 多账户路由
- **Relay 插件**：`inject ['mtproto']`，上游 `BaseTelegramClient`（`ctx.effect` 管理连接），`ctx.mtproto.fallback` 透传所有 RPC，转发上游 updates。可选 transport logger 记录明文。
- **多账户路由**（§3）：RouteRegistry + `ServerSession` 按 `routeId` 派发 + `authKeyId→route` 持久化 + 登录时按虚拟号/真实登录解析。

### ⬜ 后续
- ✅ **文件传输基础**：`upload.getFile`/`saveFilePart`、`sendMedia/sendMultiMedia`、adapter 流式 source 与进度。
- **WebUI**：用 boilerplate 自带的 webui 管理 session / 平台 / 路由。
- **测试工具复用**：把 mtproto e2e 的握手 client helper 抽成共享 test-util（现在 bridge e2e 复制了一份）。
- **平台特异功能**：自定义 emoji / 角色 / 富文本（PlatformFeatureSet）。

### 实现策略：一个 RPC 该由谁答？
- **Relay 账户**：`fallback` 全部透传给上游，几乎不用逐个实现。
- **Bridge 账户**：`help.*`/`auth.*`/`updates.*` 合成；`messages.*`/`users.*` 走平台适配；不支持的按能力声明返回空或错误。
- 「缺一个补一个」不可持续 —— Bridge 要按官端启动会调的一批（config/dialogFilters/getState/getDialogs/getUsers/contacts…）成体系地实现。

---

## 6. 已知坑（记录以免重犯）
- **手机号 `+` 前缀不一致**：客户端 `auth.sendCode` 发 `+999…`，`auth.signIn` 发 `999…`（无 `+`）！服务端必须统一 `replace(/\D/g,'')` 归一化再查/存库，否则 signIn 查不到 → `PHONE_NUMBER_UNOCCUPIED` → 登录卡死。（官端"输对码卡住"真因）
- **官端登录后先拉 `messages.getDialogFilters`**：不实现它会卡在加载界面进不去。
- **Telegram RSA 指纹**必须 `hex(reverse(sha1(TL(n)+TL(e))[-8]))`（补零 16 位），不能用 int64 `.toString(16)`（丢前导零）。
- **`boolTrue`/`boolFalse` 不在 mtcute writer map**：`_sendRpcResult` 已特判、按构造子 id 手写。
- **裸 `Vector` 返回**（`users.getUsers` 等）不能当普通 TL 对象写；handler 必须用 `bareVector(items)` 包装，由会话层写入 vector constructor/count/items。
- **可选 string 字段传 `undefined` 不传 `null`**（mtcute writer 把 null 当"存在"写 string 会崩）。
- **`ctx.model` 与 `ctx.database` 是两个注入名**，都要 `inject`。
- **`future_salts` 构造子 id = 0xAE500895**（曾误写成 0xAE5008F7，严格客户端如 mtcute 会重连打断）。

---

## 7. 运行 / 测试 / 调试

```bash
# 开发（HMR）
pnpm dev                       # cordis run，mtproto 监听 4430、HTTP 3140

# app.yml 已用 Cordis entry `id: static` 注册参考平台
curl -X POST http://127.0.0.1:3140/api/auth/static/complete \
  -H 'content-type: application/json' \
  -d '{"credentials":{"t":"x"},"metadata":{"firstName":"Alice"}}'
# → { "virtualPhone": "+999…", "loginCode": "……" }
# 官端用该号 + 验证码登录（一个号一次性，重登换新号）

# 测试
pnpm --filter @mtproto-relay/mtproto exec vitest run
pnpm --filter @mtproto-relay/bridge exec vitest run
pnpm --filter @mtproto-relay/test-suite exec vitest run

# 调试官端：抓 ~/Library/Application Support/materialgram/DebugLogs/mtp_*.txt
#   看 signIn 之后新的 METHOD_NOT_IMPLEMENTED: xxx，据此补 handler
```

客户端补丁（RSA key + DC 重定向到本机）：见旧的 `patch-tdesktop.cjs` 思路（patch 内嵌的 Telegram prod RSA PEM + DC IP/端口）。
