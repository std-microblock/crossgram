#!/usr/bin/env sh

if git interpret-trailers --parse "$1" | grep -Eiq '^[[:space:]]*co-authored-by:[[:space:]]*claude([[:space:]]|<|$)'; then
  echo 'Commit rejected: Claude must not be listed as a co-author.' >&2
  exit 1
fi
