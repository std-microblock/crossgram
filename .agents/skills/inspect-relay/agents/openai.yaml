interface:
  display_name: "Inspect Crossgram Runtime"
  short_description: "Deploy temporary TypeScript probes into Crossgram"
  default_prompt: "Inspect the live Crossgram process with a short-lived debug script, collect only the necessary structured evidence, and remove the probe afterward."
